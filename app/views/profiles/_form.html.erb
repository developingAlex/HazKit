<%= form_with(model: profile, local: true) do |form| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- shrine upload button goes here -->
  <div class="btn btn-secondary">Upload new profile photo</div>


  <div class="field">
    <%= form.label :what_is_your_name? %>
    <%= form.text_field :name, id: :profile_name %>
  </div>

  <div class="field">
    <%= form.label :where_do_you_live? %>
    <%= form.text_field :home_location, id: :profile_home_location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
